# Claude Code demo

Use shadow mode first to show proposed pruning decisions and the deletion
manifest. A release demo should then disable shadow mode, trigger Claude’s
native compaction lifecycle, and show the validated pruned transcript or host
fallback.

Do not reuse private captures or claim that Claude’s generated summary was
produced by GLiNER.
